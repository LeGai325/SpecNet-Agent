进展情况：目前已经完成了“想法可行性验证”，正在进入“统一系统、补齐真实证据”的阶段。方向成立，但距离完整论文还有一轮关键工程和实验工作。

**已经完成的部分**

1. **证明问题确实存在**
   - 推测流量约占总流量的 `12%–23%`。
   - 只按 service type 设置优先级，会出现约 `35%–39%` 的 criticality 错分。
   - 多个 workflow 聚合后，关键流和推测流比例仍然动态变化。
   - 这些结果说明静态优先级确实不够。
2. **证明控制思路基本有效**
   - Controller 能选择不同的延迟、推测覆盖度和流量浪费工作点。
   - Congestion 是有效信号。
   - Slack 有作用，但效果受样本和 seed 影响，只能写成“条件性有效”。
   - 多路径 borrowing 能明显减少固定路径分区带来的损失。
   - 稳定训练降低了跨 seed 波动。
3. **开始形成可信度和安全证据**
   - 已明确 SCS 表示“推测覆盖度”，不代表真实答案质量。
   - 已发现 criticality hint 错误可能导致关键路径超时。
   - Guard 和 confidence gate 能限制错误 hint 带来的风险。
   - 多租户保护改善了恶意租户场景下的性能隔离。
4. **Workload 更接近真实场景**
   - 已形成公开数据驱动的 V3 workload。
   - 完成了数据清洗、去重、防泄漏划分和多 seed 正式实验。
   - 但 V3 仍是“真实数据校准的模拟 workload”，不是真实生产系统。

**目前最主要的缺口**

1. **系统还没有完全合并**

真实 queue、源端 fanout 控制、多路径、Guard、confidence gate 和后台服务保证，目前还没有全部运行在同一套正式代码路径中。现在更像多个已经验证过的模块，还不是完整系统。

1. **缺少真实任务质量闭环**

HotpotQA 结果还需要审计，HumanEval/MBPP 还缺正式 held-out 实验。没有这些结果，就不能证明减少 speculative coverage 后，真实回答质量或代码通过率受到什么影响。

1. **后台服务保证尚未解决**

已有实验中 background service ratio 约为 `0.105`，低于目标 `0.20`。审稿人可能认为低时延来自后台流被饿死。必须加入硬服务下限并重新实验。

1. **最终 baseline 还不够严格**

还需要严格的 no-source-control、no-learning、tuned rule、Rule/Guard/Bandit 2×2 和 oracle comparison，证明结果不是弱 baseline 或 Guard 单独带来的。

1. **缺少部署和开销证据**

还需要轻量 sidecar 或 host prototype，并测量：

- Controller 决策时延。
- CPU 和内存开销。
- 每个 workflow 的状态开销。
- Queue marking 和更新频率。
- 对系统吞吐的影响。

1. **代码和结果需要统一收敛**

当前项目根目录还不是 canonical Git repository。正式投稿前必须统一代码入口、commit、环境、数据 manifest、配置和结果目录。